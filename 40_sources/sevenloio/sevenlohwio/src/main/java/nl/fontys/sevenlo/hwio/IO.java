package nl.fontys.sevenlo.hwio;

/**
 * Combination interface for hardware that combines input and output on the same
 * port. No additional methods then those defined in Input and Output.
 *
 * @author Pieter van den Hombergh (P.vandenHombergh at fontys dot nl)
 */
public interface IO extends Output, Input {
}
